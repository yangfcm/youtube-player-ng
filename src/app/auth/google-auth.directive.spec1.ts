import { GoogleAuthDirective } from './google-auth.directive';

describe('GoogleAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleAuthDirective();
    expect(directive).toBeTruthy();
  });
});
